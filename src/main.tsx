import React from 'react';
import ReactDOM from 'react-dom/client';
import AIDevMissionControl from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AIDevMissionControl />
  </React.StrictMode>
);
